version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: video-player-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - video-player-network

networks:
  video-player-network:
    driver: bridge
